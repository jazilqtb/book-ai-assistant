A book/PDF-based AI assistant that can answer questions based on the content of books, uses PostgreSQL + pgvector for RAG, performs web searches and fetches data via tool calls, stores knowledge and history internally, provides streaming responses, and allows clients to resume the response when the connection is lost. The infrastructure and API keys may use the MCP provided. Testing is conducted via Postman.